{
  "name": "gem-optics-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive gemstone optics: refraction, Fresnel reflection, total internal reflection and dispersion.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check src/renderer.js && node --check src/render-policy.js && node --check src/optics.js && node --check src/postprocess.js && node --check src/spectrum.js"
  }
}
